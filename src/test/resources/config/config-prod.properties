# See config-qa.properties for full revision notes on why this file exists.
# NOTE: running automated UI tests against PROD is generally reserved for
# read-only smoke/sanity checks in real companies — flagged here as a
# discussion point for Module 8 (tag-based execution: @smoke vs @regression).
base.url=https://www.example.com
browser=chrome
headless=true
implicit.wait.seconds=15
explicit.wait.seconds=30
page.load.timeout.seconds=45
env=prod
